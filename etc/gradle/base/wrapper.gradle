/**
 * gradlewコマンドを利用できるようにする
 *
 * 各環境にバージョンを揃えたうえでgradleをインストールするのは大変なので、
 * ラッパーを用意し、gradle環境の統一を図る
 */

task wrapper(type: Wrapper) {
    gradleVersion = '2.1'
    jarFile = "${project.projectDir}/etc/gradle/base/wrapper/gradle-wrapper.jar"
}